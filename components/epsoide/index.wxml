<!-- 使用 wxs -->
<wxs src="./epsoide.wxs" module="epsoide" />
<view class="container">
    <text>No.</text>
    <text>{{ epsoide._handleIndex(index) }}</text>
    <text>|</text>
    <text>{{ month }}</text>
    <text>{{ year }}</text>
</view>